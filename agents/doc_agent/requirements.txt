# ── Framework ──
fastapi==0.109.0
uvicorn[standard]==0.27.0
pydantic>=2.6.0

# ── LangGraph / LangChain (VDU pipeline orchestration) ──
langgraph>=0.2.0
langchain-core
langchain-ollama
langchain-groq

# ── Database (entity schema lookups + task status) ──
asyncpg==0.29.0

# ── HTTP client (storage, orchestrator callbacks) ──
httpx

# ── PDF Processing ──
PyMuPDF>=1.24.0
Pillow>=10.0.0

# ── Schema Validation ──
jsonschema>=4.21.0

# ── Utilities ──
python-dotenv==1.0.0
